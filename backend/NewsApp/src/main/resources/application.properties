# Server
server.port=8080

# Datasource (change password/URL if different)
spring.datasource.url=jdbc:mysql://localhost:3306/adj?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=Davinder27@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA (dev)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hikari
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# CORS
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false
# Timezone
spring.jackson.time-zone=Asia/Kolkata

# News API key (only the key value)
# Option A (recommended): read from environment variable if present, otherwise use the fallback below.
# news.api.key=${NEWS_API_KEY:}
# Option B (not recommended): place the key literally here (only do this for local dev).
news.api.key=e0ffb38cad15442b8abe7172d83105dc
